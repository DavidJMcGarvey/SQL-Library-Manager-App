extends ../layout

block content

  h2.detail= title
  p 
    h4= 'Are you sure you want to delete ' + book.title + '?'

  form(action="/books/" + book.id + "/delete", method="post")
    a.joking(href="/books/" + book.id) No! I was just joking! 
    | 
    input(type="submit", value = "Yes, I'm sure")