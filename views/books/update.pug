extends ../layout

block content

  h1 Update Book
  form(action=`/books/${book.id}/new`, method='POST')
    if errors
      h2.errors.detail Oooops!
      ul.errors
        li.error *Author is required
        li.error *Name is required
        
    p
      label(for='title') Title
      input#title(name='title' type='text', value=book.title)
    p
      label(for='author') Author
      input#author(name='author' type='text', value=book.author)
    p
      label(for='genre') Genre
      input#genre(name='genre' type='text', value=book.genre)
    p
      label(for='year') Year
      input#year(name='year' type='text', value=book.year)
    p
      input(type='submit' value='Update Book')
    p
      a.button(href=`/books/${book.id}`) Cancel
