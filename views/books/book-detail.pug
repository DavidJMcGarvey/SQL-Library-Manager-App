extends ../layout

block content

  h2.detail=  book.title
  h2.detail=  book.author
  h2.detail= book.genre + '  |  ' + book.year

  br

  a.button(href=`/books/${book.id}/new`) Edit

  a.button.search(href=`/books/${book.id}/delete`) Delete

  a.button(href='/') Back